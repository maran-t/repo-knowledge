import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Editions — Ask questions about any repo's commit history",
  description:
    "Semantic search over GitHub commit histories. Ingest a repository, then ask in plain English — answers cite the actual commits.",
  openGraph: {
    title: "Editions",
    description: "Ask questions about any GitHub repo's commit history.",
    url: "https://repo-knowledge.vercel.app",
    type: "website",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
